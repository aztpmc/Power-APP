# ExportLC-12-ImportValidate

Parses an uploaded workbook, applies the same validation rules as the form, and stages every row with a validation state. Writes nothing to the live lists.

See `definition.json`. Set the `SiteUrl` parameter before enabling.
