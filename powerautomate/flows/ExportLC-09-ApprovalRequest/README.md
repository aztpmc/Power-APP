# ExportLC-09-ApprovalRequest

Routes an approval to a treasury manager for LC closure, large amendments and proceeds write-offs, and records the decision in the audit trail.

See `definition.json`. Set the `SiteUrl` parameter before enabling.
