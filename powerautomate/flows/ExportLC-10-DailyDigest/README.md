# ExportLC-10-DailyDigest

One morning email per officer covering everything on their desk, so individual reminders can be turned down without losing visibility.

See `definition.json`. Set the `SiteUrl` parameter before enabling.
