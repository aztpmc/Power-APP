# ExportLC-14-UploadDocument

Writes a file and its business metadata into the right library in one operation, so a file with no metadata cannot exist.

See `definition.json`. Set the `SiteUrl` parameter before enabling.
